## `display`

Renders text to a monitor based on the configuration set in the console.

## `displayinstaller`

Installs the display to the computer with a default configuration. The default
configuration is applied when the `display` script starts up for the first time.
